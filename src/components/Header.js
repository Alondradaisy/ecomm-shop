import React from "react";
import ShoppingCartIcon from "@mui/icons-material/ShoppingCart";

function Header() {
  return "Header";
}

export default Header;

// sx {{ }} prop <- enables you to define custom styles in Material-UI - look up documentation
// these styling themes are specific to material-ui, not CSS - similar styling, diff syntax
